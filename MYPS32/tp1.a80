.text
main:
    addi $s1, $zero, 1       # b  = 
    addi $s2, $zero, 2       #
    addi $s3, $zero, 3       #
    addi $s4, $zero, 4       # 
    add $t0, $s1, $s2        # t = 
    add $t1, $s3, $s4
    sub $s0, $t0, $t1
    jr $ra                   # fin du premier ex
    bne $s0, $zero, 0        # if a != 0
    add $s1, $s2, 2          # b = c + 2
    beq $s0, $zero, 0        # if a == 0
    add $s3, $s3, $s4        # d = d + e
    jr $ra                   # fin ex deux
           

